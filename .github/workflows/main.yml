# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push 
  push:
    branches:
      - main
  pull_request:
    types:
      - closed
    branches:
      - main

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3
      
      - name: Install build dependencies
        run: pip install PyYAML

      - name: Build the packages and repo structure
        run: |
          export GNUPGHOME=$GITHUB_WORKSPACE/.gnupg
          mkdir -p $GNUPGHOME
          gpg --import <(echo "${{ secrets.GPG_KEY }}")
          gpg --list-keys
          python scripts/build_packages.py
          bash scripts/make_repo.sh

      - name: Add and commit the changes
        run: |
          pwd
          ls -laR
          git add repo/dists/stable/main/binary-amd64/Packages
          git add repo/dists/stable/main/binary-amd64/Packages.gz
          git add repo/dists/stable/Release
          git add repo/dists/stable/Release.gpg
          git add repo/dists/stable/InRelease

          for i in `ls repo/pool/*.deb`; do
            git add $i
          done

          git commit -m new repo structure
          git push origin main
          git push origin `git subtree split --prefix=repo main --ignore-join --rejoin`:gh-pages --force

